[pytest]
# tools/smoke_test.py matches pytest's default *_test.py pattern, but it is the
# paid API distinctiveness harness, so only collect test_*.py files.
python_files = test_*.py
testpaths = tools/tests
